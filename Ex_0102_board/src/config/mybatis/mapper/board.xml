<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="b">
	<select id="board_list" resultType="board">
		SELECT * 
		FROM BOARD 
		ORDER BY 
			ref DESC,step ASC
	</select>
	<!-- 새글 추가 -->
	<insert id="board_insert" parameterType="board" >
		INSERT INTO BOARD 
		VALUES(
			seq_board_idx.nextVal,
			#{name},
			#{subject},
			#{content},
			#{pwd},
			#{ip},
			sysdate,
			0,
			seq_board_idx.currVal,
			0,
			0
		)
	</insert>
	<select id="board_list_one" resultType="board">
		SELECT * 
		FROM BOARD 
		WHERE idx=#{a}
	</select>
	<delete id="board_delete" parameterType="int">
		DELETE FROM WHERE idx=#{a}
	</delete>
	<update 
		id="board_update"
		parameterType="board">
		UPDATE board 
		SET
			subject = #{}
			content = #{}
			
		WHERE 
			idx= #{}	
	
	</update>
</mapper>